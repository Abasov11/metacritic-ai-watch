Шаг 4b — исправления по итогам приёмки шага 3 (я поднял сервер и снял скриншоты в headless-браузере: вёрстка хорошая, но есть дефект).

1. **Обложки не отображаются — все `<img>` дают 403.** Причина: `_cover_url` берёт подписанный `/a/img/resize/<hash>/…` адрес, но отбрасывает query-параметры (`?auto=webp&fit=cover&height=…&quality=…&width=…`), а подпись их покрывает. Проверил curl'ом: подписанный без параметров → 403, неподписанный `/a/img/catalog/provider/7/2/7-1781631535.jpg` → 200 image/jpeg (180 КБ). Сделай надёжно: при обходе скачивай обложку в `data/covers/<slug>.<ext>` (лимит 5 МБ, только image/*, через тот же вежливый клиент), в `games` храни и `cover_url` (оригинал, неподписанный вариант — предпочтительно), и `cover_path`; отдавай через `GET /covers/<slug>.<ext>` (StaticFiles или свой роут с проверкой имени файла — без path traversal); в шаблонах — локальный путь, если файл есть, иначе `cover_url`, иначе заглушка. Перекачивать, если файла нет или он старше 30 дней. Для уже лежащих в базе 4 игр — команда `python -m app.crawler --refresh-covers`, прогони её вживую и покажи `ls -la data/covers`.
2. HTMX положи в `app/static/htmx.min.js` (версия 1.9.12, скачай с unpkg один раз, проверь sha256 против опубликованной) и подключай локально — сервис не должен зависеть от чужого CDN.
3. Скриншот на 375 px я тоже сниму сам — но проверь по CSS, что сетка карточек на 375 px в одну колонку, а фильтр платформ переносится.
4. Тесты: обложка кэшируется (мок HTTP), путь в шаблоне локальный; недопустимое имя файла в `/covers/` → 404.

Коммить по частям. В конце — отчёт: сделано / проверено запуском / не получилось.
