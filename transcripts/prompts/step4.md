Уточнение по шагу 2: `docs/MODEL_CHOICE.md` в репозитории есть (коммит a2caa33), просто ты проверял до него — перечитай при необходимости. Решение с выводом номера страницы из `crawl_runs` принимаю. Ещё: повесь `similar.invalidate()` на конец обхода, как сам предлагал.

Шаг 4 — дополнительная часть 2 из `docs/TASK.md`: мониторинг работы сервиса в реальном времени и кнопка принудительного запуска. Код — через `/ponytail`, интерфейс — в стиле уже сделанных страниц (`/ui-ux-pro-max` при необходимости).

Что нужно:
1. `app/monitor.py` — единая шина событий в процессе: `emit(event: dict)` (тип, время, воркер, слаг, сообщение, счётчики); хранит последние 200 событий в памяти и текущее состояние: воркеры (`crawler`, `llm`, `youtube` — что делает сейчас: idle / fetching <slug> / summarizing <slug> critic …), текущий run (source, planned/processed/failed, начат), итоги за сегодня (обходов, игр, резюме, ошибок, стоимость LLM из `llm_calls`). Крауler и LLM-клиент шлют события в узловых точках (начало/конец обхода, каждая игра, каждый вызов LLM с ms и cost, каждая ошибка).
2. `GET /monitor` — страница: карточки состояния воркеров, прогресс текущего обхода, счётчики за сегодня, таблица последних обходов (`crawl_runs`), живой лог событий. Обновление без перезагрузки через SSE `GET /monitor/stream` (heartbeat раз в 15с, переподключение на клиенте). Без JS-фреймворков — ванильный `EventSource`.
3. `POST /monitor/run` — кнопка «Запустить обход сейчас»: если обход уже идёт — ответ 409 и сообщение в UI, иначе запуск в фоне (через планировщик/поток) и немедленный ответ; кнопка блокируется, пока идёт обход. Защита от случайных повторных нажатий (дизейбл + серверный замок из шага 2).
4. Ссылка на «Мониторинг» в шапке всех страниц, `/healthz` расширить состоянием воркеров.
5. Тесты: события попадают в буфер и в состояние; `POST /monitor/run` → 202 и 409 при занятом замке; `/monitor/stream` отдаёт первое событие-снимок состояния (проверить через TestClient со стримингом, с таймаутом).
6. Живая проверка: подними сервер, нажми кнопку curl'ом (`POST /monitor/run`), параллельно `curl -N /monitor/stream` и покажи 15–20 строк реальных событий обхода; повторный POST во время обхода → 409. Сервер потом останови.

Коммить по частям. В конце — отчёт: сделано / проверено запуском / не получилось.
